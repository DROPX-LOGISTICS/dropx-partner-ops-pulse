# Cloudflare Workers — not used (free)

This Next.js app exceeds Cloudflare Workers **free** size (≈3.2 MiB gzip vs 3 MiB max).

**Deploy on Render instead:** [RENDER_DEPLOY.md](./RENDER_DEPLOY.md)

No Cloudflare paid plan was purchased. Any Worker stub created during testing was deleted.
