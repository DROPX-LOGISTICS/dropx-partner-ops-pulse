# Hosting

**Use Render (free).** See [docs/RENDER_DEPLOY.md](docs/RENDER_DEPLOY.md).

Cloudflare Workers free tier cannot fit this Next.js app (~3.2 MiB Worker gzip vs 3 MiB free limit).  
No Cloudflare paid plan was used; leftover Worker resources on the DropX account were removed.
